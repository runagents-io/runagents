# Swagger UI

Use this page when you want to inspect and navigate the public RunAgents REST API interactively from the canonical OpenAPI contract.

!!! info "Reference Views"
    [:material-file-code-outline: OpenAPI Contract](openapi.md){ .md-button }
    [:material-book-open-variant: Redoc Reference](redoc.md){ .md-button }

    Swagger UI is best for interactive exploration and deep-linking into specific operations. For filtered, per-resource views, use the generated links at the top of each API resource page in this section.

The UI loads the synchronized docs-site copy of the canonical spec:

```text
/api/openapi.yaml
```

<div id="swagger-ui" data-spec-url="../openapi.yaml"></div>
